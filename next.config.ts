import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'encrypted-tbn2.gstatic.com', 'm.media-amazon.com', 'encrypted-tbn0.gstatic.com']
  }
};

export default nextConfig;
